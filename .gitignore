# env / secrets
.env
.env.local
.env.*.local
!.env.example

# node
node_modules/
dist/
.vite/
*.tsbuildinfo

# go
/backend/bin/
*.exe
*.test
*.out

# python parser sidecar (phase 2, see docs/00-DESIGN.md 4.2)
__pycache__/
*.py[cod]
.venv/
venv/

# data / uploads / models
/data/
/uploads/
/models/

# Exam papers are the university's copyright, not ours. Never commit a corpus.
# The product is designed so the corpus stays on the operator's own machine.
*.pdf
!docs/**/*.pdf
# Exception: a synthetic exam written for this repository, used to test the
# parser against a genuine PDF rather than a hand-built fixture.
!backend/internal/ingest/testdata/*.pdf

# tooling
.idea/
.vscode/
.DS_Store
Thumbs.db
coverage/
